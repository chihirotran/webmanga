<? 
session_start();
include('../config.php');
$query = query("SELECT * FROM oder");
	confirm($query);
	$data = array();
while ($row = fetch_array($query)) {
  $data[] = array(
    'price' => $row['price'],
    'id_user' => $row['id_user']
  );
}

// Chuyển định dạng dữ liệu sang JSON
$data_json = json_encode($data);
echo json_encode($data);
?>